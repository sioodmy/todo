fn main() {
	// SPEC:
	//	Adding & Removing
	//	Finishing (maybe also unfinishing(via undo command?)) (the same as Removing?)
	//	serialized via toml
	//	Descriptions
	//	Listing (displaying to Stdout)
}
